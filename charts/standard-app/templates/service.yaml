apiVersion: v1
kind: Service
metadata:
  name: {{ include "standard-app.fullname" . }}
  labels:
    {{- include "standard-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "standard-app.selectorLabels" . | nindent 4 }}
