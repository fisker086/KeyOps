package storage

import (
	"github.com/fisker086/keyops/internal/bastion/types"
)

// 类型别名，保持向后兼容
type CommandRecord = types.CommandRecord
type SessionRecord = types.SessionRecord
type LoginRecord = types.LoginRecord
type ProxyInfo = types.ProxyInfo
